# 核心依赖 - 深度学习与大模型
torch>=2.9.0
transformers>=5.3.0
accelerate>=1.11.0
tokenizers>=0.22.0
safetensors>=0.6.0
huggingface-hub>=1.6.0
sentencepiece>=0.2.0

# 日志与进度
loguru>=0.7.3
tqdm>=4.67.0

# 向量库 (两个都使用: FAISS 为默认, LanceDB 为可选后端)
faiss-cpu>=1.13.0
lancedb>=0.30.0

# 数值与文本处理
numpy>=2.1.0
scikit-learn>=1.6.0
requests>=2.32.0
