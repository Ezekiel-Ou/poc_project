# 统一放在项目下的模型目录，支持跨机器复线
VEC_MODEL_PATH=./models/simcse-chinese-roberta-wwm-ext
LLM_PATH=./models/qwen3-1.7b

# 向量索引配置
VEC_INDEX_DATA=vec_index_intent_few
VEC_INDEX_ROOT=./llm_classification/data/index
VEC_DB_TYPE=faiss

# 数据路径配置
CLASS_DEF_PATH=./llm_classification/data/intent_data/class_def.tsv
SOURCE_INDEX_DATA_PATH=./llm_classification/data/intent_data/train.jsonl
TEST_DATA_PATH=./llm_classification/data/intent_data/test_set_intent_few.jsonl

# 批量推理 batch 大小（CPU/低内存 MPS 建议 2-4，24GB+ GPU 可调到 16-32）
BATCH_SIZE=8
